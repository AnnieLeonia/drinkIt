<!DOCTYPE HTML>
<html>
  <% include head %>

  <body>
    <h1>Welcome to <%= drinkName %>!</h1>

     <ul>
        <% recipeitems.forEach(function(recipeitem) { %>
            <li>
                <%= recipeitem.amount %>
                <%= recipeitem.ingredient %>
            </li>
        <% }) %>
    </ul>

  </body>
</html>